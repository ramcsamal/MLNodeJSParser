# SSL/TLS Configuration
# Set to '0' to disable SSL verification (useful for corporate networks with self-signed certificates)
NODE_TLS_REJECT_UNAUTHORIZED=0

# Model cache directory (optional)
# TRANSFORMERS_CACHE=./models

# HuggingFace mirror (optional, for regions with restricted access)
# HF_ENDPOINT=https://hf-mirror.com
